// few topics are discussed here of lecture week 1:-4
// callback(in details), map, filter,find, sort, will be discussed later.

// forEach
const initialArray= [1,2,3,4,5];

function logThing(str){
    console.log(str);
}

// [1,2,3,4,5]
initialArray.forEach(logThing);
// this line means forEach runs, function logThing for 
// every element of array initialArray(here).

